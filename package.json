{
  "name": "game-time-plus-monorepo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev:game-time-plus": "pnpm --filter game-time-plus dev",
    "dev:server": "pnpm --filter game-time-plus-tinybase-server dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  }
}
